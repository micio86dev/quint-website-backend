{
  "name": "quint-website-backend-tests",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": { "test": "node --test --experimental-test-coverage" },
  "engines": { "node": ">=22" }
}
